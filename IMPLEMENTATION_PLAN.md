# Implementation Plan

1. Build five small, deterministic legal-AI demonstrations with synthetic inputs, standard-library Python, project tests, and measured JSON results.
2. Connect the demonstrations to all 24 curriculum lessons through an English course map and a machine-readable evidence graph pinned to Microsoft commit `392d0df1b2647cbee104942390551f1ed9e072c8`.
3. Document provenance, authorship, AI assistance, privacy, human review, intellectual property, limits, and recruiter-facing verification.
4. Add one repository validator, `unittest` discovery, and GitHub Actions behind `python scripts/run_checks.py`.
5. Leave the independent recruiter report explicitly pending for a context-free QA agent; initialize local Git only after all implementation checks pass.

The repository supports a self-declared course review and verified post-course artifacts. It does not claim a credential, official grade, or completion of every official assignment.
