# Smoke tests

Run `python scripts/run_checks.py` from the repository root. The runner validates structure, evidence, links, claims, safety checks, saved outputs, and all five project test suites without network access or third-party packages.
